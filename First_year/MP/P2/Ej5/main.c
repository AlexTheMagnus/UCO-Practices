#include <stdio.h>
#include <string.h>

main(){
  char frase[]="Hola pepe me llamo marco";
  int size=strlen(frase);
  printf("Frase: %s, Longitud: %d \n", frase, size);

  int npalabras = 1;
  for (int i = 0; i < size; i++) {
    if( = " "){
      npalabras++;
    }
  }

  char *palabras[npalabras];
  for (int i = 0; i < size; i++) {
    if(isspace(frase[i])){
      
    }
  }




  for(int j=0, j<n)
}
